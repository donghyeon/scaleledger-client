[project]
name = "scaleledger-client"
version = "0.1.0"
description = "The headless IoT client software for the ScaleLedger fishery auction system."
readme = "README.md"
requires-python = ">=3.14"
dependencies = []
